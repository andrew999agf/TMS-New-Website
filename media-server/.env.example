# Copy to ".env" (same folder) and fill in. Never commit the real .env.

# Cloudflare Tunnel token (Zero Trust → Networks → Tunnels → your tunnel).
TUNNEL_TOKEN=

# Your home public IP (https://ifconfig.me). Used for WebRTC ICE candidates.
# Re-run `docker compose up -d` if your ISP changes it.
PUBLIC_IP=
